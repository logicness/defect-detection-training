# 下一步待办（明日续训交接单）

**日期**: 2026-09-08（晨）
**当前状态**: ✅ 诊断全部完成；⏸️ 模型未动（部署模型仍是 prod_v2_b2）；待执行「标注重构 + v2.1 续训」

---

## 一、昨日结论速览（详见 `诊断报告_20260907_模型评估与优化方向.md`）

1. **不误识别已达标**：200 张纯背景图 0 误检框（100% 干净）。
2. **主要矛盾是漏检**：8 个类漏检率 >50%，最重 Waist folding 漏检 77.9%。
3. **Waist folding 病因=标注粒度异常**（不是数据少）：78% 框面积 <1%，单图最多 228 个碎片框。
4. **合并验证通过**：margin=0.06 下碎片框 228→9，框面积中位 0.27%→5.81%（已超线状强类 Welding line 的 4.3%）。

---

## 二、今日三步待办

### 第 1 步：标签重构（~5 分钟，纯 CPU）
用 `scripts/diag/merge_fragmented_labels.py`（margin=0.06）+ 最小框膨胀，把 production 全量 train/val 的 Waist folding 重构为 **production v2.1 副本**（不动原数据）。

要点：
- 只重建 class_id=21（Waist folding），其余 38 类标签原样复制。
- 输出新数据集目录（建议 `datasets_master/production/v2.1/`），并生成对应 `data.yaml`（沿用 39 类 names）。
- 重构后跑 `scripts/diag/audit_annotation.py` 复核：目标「极小框 <15% 且 框/图 ≈2」。
- 建议追加一步「最小框膨胀」：对合并后仍 <1% 面积的框，膨胀到最小高度 5%（可选，脚本可加 `--min-h` 参数）。

### 第 2 步：v2.1 续训（~1~1.5 小时，GPU 后台）
从 `runs/detect/prod_v2_b2/weights/best.pt` 低学习率续训 2~3 blocks（每 block 5 epochs），同步：
- 弱类重采样：warp / Rolled pit / Crease 3~5×；
- 混淆对难例：Bright scratch↔Pitted surface、Oil/Water spot、slag_skin↔water_slag_mark。
- 参照现有 `scripts/train/train_production_v2.py` 的分块续训逻辑改一份 `train_production_v21.py`。

### 第 3 步：三重验收（~15 分钟，达标才升级）
1. 39 类全量评估：`scripts/diag/diagnose_confusion.py --yaml production/v2.1/data.yaml`；
2. 纯背景误检：`scripts/diag/test_background_fp.py`（目标仍 0 误检）；
3. Waist folding 专项：审计 + mAP50 对比，目标 recall 22%→≥60%。

---

## 三、验收标准

| 指标 | 当前 | 目标 |
|---|---|---|
| 39 类 mAP50 | 0.79 | ≥0.84 |
| Waist folding recall | 22.1% | ≥60% |
| 其余弱类 recall | 30~54% | 各 ≥65% |
| 纯背景误检 | 0/200 | 保持 0 |

---

## 四、关键路径速查

| 项目 | 路径 |
|---|---|
| 部署模型（当前） | `runs/detect/prod_v2_b2/weights/best.pt` |
| v11m 底座 | `runs/detect/univ39_v11m_b20/weights/best.pt` |
| 训练数据 v1.0 | `datasets_master/production/v1.0/` |
| 合并脚本 | `scripts/diag/merge_fragmented_labels.py` |
| 评估/诊断脚本 | `scripts/diag/`（5 个脚本均可复用） |
| Python 环境 | `E:\Anaconda\envs\yolov11\python.exe`（注意路径含 `&` 需引号） |

---

## 五、本次诊断产物（保留）

| 文件 | 说明 |
|---|---|
| `诊断报告_20260907_模型评估与优化方向.md` | 完整诊断 + 计划（主文档） |
| `训练记录_20260906_缺陷检测模型训练完成.md` | 已更新（纠正误判 + 刷新计划） |
| `scripts/diag/*.py` | 5 个可复用脚本 |
| `scripts/diag/*.json` | 逐类审计 / 混淆矩阵 / 背景误检证据 |
| `scripts/diag/merged_m006/` | 已验证的最佳合并副本（margin=0.06） |