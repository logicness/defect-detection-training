# 模型评估与优化方向诊断报告

**日期**: 2026-09-07
**诊断对象**: prod_v2_b2 (YOLO11m, 39类)
**诊断口径**: 生产集 production v1.0 val（5,296 图 = 5,096 有缺陷 + 200 纯背景负样本；11,182 个标注框）
**诊断人**: MiMo

---

## 一、本次诊断做了什么

1. **修正评估口径**：发现此前正式评估只覆盖 35 类（`um39_val_35c`），其中 4 个新类 blowhole / break / fray / uneven **从未被评估过**。本次改用生产集 v1.0 自带的 39 类 val 做全量评估。
2. **逐类样本审计**：统计每个类的训练框数、覆盖图片数、每图框密度（产出 `scripts/diag/class_balance_audit.json`）。
3. **数值混淆矩阵**：提取全量 40×40 混淆矩阵，拆解为「混淆对 / 漏检 / 误报」三类问题（产出 `scripts/diag/confusion_prod_v2_b2_39c_c025.{json,md}`）。
4. **纯背景误检专项测试**：对 200 张无缺陷图单独跑推理，检验"没缺陷是否会乱框"（产出 `scripts/diag/background_fp_prod_v2_b2.json`）。

---

## 二、核心结论（三句话）

1. **"不误识别"在"无缺陷"层面已经达成**：200 张纯背景图 **0 误检框、100% 干净**。模型不会在干净金属/无缺陷图上乱框。
2. **最大的问题是"漏检"，不是"误报"**：整体 recall 仅 65.5%（11,182 个真实缺陷框漏掉 3,857 个）。有 8 个类漏检率 > 50%，最严重的 Waist folding 漏检率高达 **77.9%**。
3. **4 个新类首次评估结果：3 优 1 中**。blowhole(recall 98.7%)、fray(97.2%)、uneven(96.7%) 都很强；break 漏检 51%（但精度 96.9%，是"漏检型"而非"误报型"）。

---

## 三、39 类全量评估结果（首次补全）

| 口径 | 评估集 | 类别数 | mAP50 | mAP50-95 | P | R |
|---|---|---|---|---|---|---|
| 旧口径 | um39_val_35c | 35 | 0.7855 | 0.5660 | 0.779 | 0.728 |
| **新口径(补全)** | production v1.0 val | **39** | **0.79** | **0.56** | 0.789 | 0.729 |

> 注：两套口径评估集不同，不可直接横向比较。新口径首次把 4 个新类纳入，且含 200 张负样本，更接近真实部署。

**混淆矩阵（conf=0.25，操作口径）**：GT 11,182 框 → 模型输出 9,714 框 → 正确 7,325（recall 65.5% / precision 75.4%）→ 漏检 3,857、误报 2,389（全部来自"有缺陷图上的多余框"，纯背景 0 误报）。

---

## 四、混淆矩阵分析

### 4.1 Top 混淆对（GT 类被判成其它类）

| GT类 | 被错判为 | 次数 | 说明 |
|---|---|---|---|
| Bright scratch | Pitted surface | 39 | 划痕 vs 麻点 双向混淆(回向34) |
| Pitted surface | Bright scratch | 34 | 同上，双向 |
| Oil spot | Inclusion | 12 | 油斑 vs 夹杂(流体斑点) |
| Water spot | Oil spot | 11 | 水斑 vs 油斑 |
| water_slag_mark | slag_skin | 3 | 水渣痕 vs 渣皮 |
| Rolled pit | Crease | 4 | 轧坑 vs 折痕 |
| warp | Dark scratches | 2 | 翘曲 vs 暗划痕 |
| break | fray | 9 | 断裂 vs 磨损(新类间) |

### 4.2 漏检榜 Top8（GT 有缺陷但模型没框出来 —— 决定"准确度"）

| 类名 | 漏检数 | GT数 | 漏检率 |
|---|---|---|---|
| **Waist folding** | 898 | 1153 | **77.9%** |
| **warp** | 14 | 24 | 58.3% |
| **Crazing** | 289 | 529 | 54.6% |
| **Rolled pit** | 28 | 52 | 53.8% |
| **Bright scratch** | 517 | 1010 | 51.2% |
| **break** | 175 | 343 | 51.0% |
| **Crease** | 31 | 65 | 47.7% |
| **Pitted surface** | 342 | 747 | 45.8% |

### 4.3 误报榜 Top5（模型多框出来的框 —— 但因纯背景 0 误检，全部落在"有缺陷图"上）

| 类名 | 误报数 | 精度 |
|---|---|---|
| Inclusion | 645 | 65.7% |
| Bright scratch | 235 | 61.7% |
| Waist folding | 227 | 52.9% |
| Slag inclusion | 149 | 56.9% |
| Patches | 144 | 81.6% |

---

## 五、弱类根因分类（逐类定性 + 证据）

### A. 标注异常型（数据多却最差 —— 优先审标注，不要盲目加数据）

| 类 | 关键证据 | 判定 |
|---|---|---|
| **Waist folding** | 训练框 3,009/仅 262 图 = **11.5 框/图**；val 15 框/图；recall 仅 22.1% 且 FP 也高(52.9%)；**不在任何混淆对里**（没被错判成别的类，就是纯粹检不出也乱框） | 标注粒度/一致性异常，模型学不到稳定模式 |

### B. 样本不足型（train 框 < 200，加数据/增强最有效）

| 类 | train框/图 | recall | 判定 |
|---|---|---|---|
| **warp** | 124/110 | 29.2% (精度100%) | 样本少，纯漏检 |
| **Rolled pit** | 164/71 | 30.8% | 样本少 + 与 Crease 混淆 |
| **Crease** | 174/96 | 46.2% | 样本少 |

### C. 类别混淆型（数据够但互相搞混）

| 类 | 证据 |
|---|---|
| **Bright scratch** | 与 Pitted surface 双向混淆(39+34)，两者都中招 |
| **Slag inclusion** | 与 slag_skin 混淆、FP 149、recall 65.8% |
| **Pitted surface** | 与 Bright scratch 混淆、recall 49.4% |

### D. 本质难检型（细裂纹/形态多样）

| 类 | train框/图 | recall | 判定 |
|---|---|---|---|
| **Crazing** | 2214/1274 | 45.0% | 数据足够，细裂纹本身难检 |
| **break** | 2618/954 | 45.8% | 数据足够，断裂形态多样，精度却高达 96.9%（纯漏检） |

### E. 类别过宽型

| 类 | 证据 |
|---|---|
| **Inclusion** | 最大类(train 6,100框)，FP 645 个（过检最多），吸收了大量相近缺陷 → 需要边界细化或负例约束 |

---

## 六、优化方向（更新后的计划）

### 总体策略

**一次「弱类定向强化重训练 v2.1」同时服务两个目标**：漏检类提召回（准确度）+ 混淆对拆解（精度），全部用本地已有数据，从 prod_v2_b2 续训（非从头），不动部署模型直到验证通过。

### P0（优先，直击两大短板）

| 顺序 | 动作 | 针对 | 预期 |
|---|---|---|---|
| 1 | **Waist folding 标注审计**：抽样可视化检查 11.5 框/图的标注是否过密/误标/粒度不一致；必要时清洗或合并框、统一粒度 | 最大漏检类 | recall 22%→≥60% |
| 2 | **样本不足类重采样+定向增强**：warp/Rolled pit/Crease 做 3~5× 重采样 + 锐化/对比度/光照/尺度扰动 | 三类样本不足 | recall 30~46%→≥65% |
| 3 | **混淆对难例重训 + 本质难检类增强**：Bright scratch↔Pitted surface、Oil/Water spot、slag_skin↔water_slag_mark；Crazing/break 加强细裂纹/多尺度增强 | 混淆类 + 难检类 | recall 45~54%→≥65% |
| 4 | **续训 v2.1**：低学习率 fine-tune 2~3 blocks | 全局 | mAP50 0.79→0.84~0.86 |

### P1（防误识别巩固）

| 动作 | 针对 | 预期 |
|---|---|---|
| 5 | **Inclusion 边界细化**：对 FP 过的 Inclusion 加难负例（有缺陷但不是 Inclusion 的图） | 过检类 | precision 65.7%→≥75% |
| 6 | **重训后重校每类高精度阈值**（保持五道门控） | 部署防误报 | 纯背景保持 0 误检 |

### P2（体系化，长期）

| 动作 | 说明 |
|---|---|
| 7 | 把「39 类全量评估 + 纯背景误检专项 + 混淆对」固化成每轮训练的**标准验收**，三者全绿才升级模型 |
| 8 | APDDD 铝材数据（本地 3,770 张）单独建"铝材线"，不与钢铁主线混训，避免稀释精度 |

### 量化验收目标

| 指标 | 当前 | 目标 |
|---|---|---|
| 整体 mAP50（39类） | 0.79 | ≥0.84 |
| 8 个漏检类 recall | 22~54% | 各 ≥ 65% |
| 纯背景误检 | 0/200 | 保持 0 |
| 过检类 Inclusion precision | 65.7% | ≥ 75% |

---

## 七、附录：本次诊断产物

| 文件 | 说明 |
|---|---|
| `scripts/diag/class_balance_audit.json` | 逐类训练框数/图片数/框密度 |
| `scripts/diag/confusion_prod_v2_b2_39c_c025.json` | 全量 40×40 数值混淆矩阵 |
| `scripts/diag/confusion_prod_v2_b2_39c_c025.md` | 混淆对/漏检/误报可读报告 |
| `scripts/diag/background_fp_prod_v2_b2.json` | 纯背景 0 误检证据 |
| `scripts/diag/diagnose_confusion.py` | 混淆矩阵诊断脚本（可复用） |
| `scripts/diag/audit_class_balance.py` | 逐类样本审计脚本（可复用） |
| `scripts/diag/test_background_fp.py` | 纯背景误检专项脚本（可复用） |

---

## 八、P0-1 执行结果：Waist folding 标注审计（2026-09-07 已完成）

### 8.1 结论

**Waist folding 的低 recall / 低 mAP 主要是标注粒度异常，不是数据不足，也不是模型本质"看不见"。**

### 8.2 证据（与强类基线对照）

| 指标 | Waist folding(最弱) | Inclusion(过检) | Crescent gap(强) |
|---|---|---|---|
| mAP50 | 0.311 | 0.738 | 0.995 |
| 框/图(train) | **11.48** | 2.91 | **1.0** |
| 框面积中位 | **0.27%** | 1.06% | **9.08%** |
| 极小框(<1%面积) | **78.2%** | 48.7% | **2.2%** |
| 每图框数分布 | 两极(122张各1框 ↔ 个别图86~228框) | 连续 | 恒为1 |

### 8.3 病因

1. **框极小**：78% 的框面积 <1%，中位框高仅占图高 2.3%。在 IoU 0.5 下几乎无法命中（几像素定位误差就能让 IoU 跌破 0.5）。
2. **标注粒度两极分化**：同一类同时存在"单实例图"（train 122 张各 1 框）和"密集碎片图"（个别图 86~228 框），模型学不到稳定模式。
3. **train 与 val 同样碎片化**（val 极小框占 78.5%）→ 连评估都对这个类不公平。

> 注：该问题源自上游 SteelDefectX 源标注（`um_um_sdx_wf_*`）的原始粒度，非本工程转换所引入。

### 8.4 处置建议（需批准后执行）

1. **标注重构（首选）**：对 Waist folding 自动"合并碎片框 → 折叠区域/折叠线级粗框"（簇聚类或形态学膨胀合并），train + val 同步重构后再重训；这是唯一能真正解这个类的路子。
2. **配合弱类重采样**：合并后按适中框数重采样，避免两极。
3. 备选：若暂不动标注，可对该类单独用 IoU 0.3 评估（只衡量"能否找到折叠区域"），但训练收益有限。

**相关数据**：`scripts/diag/annotation_audit_Waist_folding.json`、`annotation_audit_Inclusion.json`、`annotation_audit_Crescent_gap.json`，渲染样本图见 `scripts/diag/annotation_renders/`。

### 8.5 合并验证结果（2026-09-07 副本试跑，未动原数据）

写并跑通 `scripts/diag/merge_fragmented_labels.py`（几何连通分量合并，三档 margin 对比）：

| 指标 | 原始 WF | WF m0.04 | **WF m0.06** | Welding line(强·线状) | Crescent gap(强·块状) |
|---|---|---|---|---|---|
| 框/图(train) | 11.48 | 3.08 | **2.26** | 1.01 | 1.0 |
| 框面积中位 | 0.27% | 1.48% | **5.81%** | 4.3% | 9.08% |
| 极小框(<1%面积) | 78.2% | 44.2% | **31.8%** | 2.4% | 2.2% |
| 每图框数 max | 228 | 11 | **9** | 3 | 2 |

**结论**：margin=0.06 是最佳档——中位框面积 5.81% **已超过同为线状缺陷的强类 Welding line(4.3%)**，病态碎片化（单图 228 框）彻底消除（→9）。残余 31.8% 极小框是"孤立单条细折痕"的固有形态，可再用"最小框膨胀"二次处理。

**验证产物**：`scripts/diag/merged_m{002,004,006}/`（合并副本）、`annotation_audit_Welding_line.json` 等基线 JSON、渲染对比图。

---

**下一步（需批准，耗时较长）**：
1. 用 m0.06 + 最小框膨胀，把 production 全量标签（train+val 的 Waist folding）重构成 production v2.1 副本；
2. 从 prod_v2_b2 续训 v2.1（低学习率、2~3 blocks），同步做弱类重采样 + 混淆对难例；
3. 训完跑「39 类全量评估 + 纯背景误检 + Waist folding 专项」三重验收，达标才升级。