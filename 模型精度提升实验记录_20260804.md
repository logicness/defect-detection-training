# NEU 模型精度提升实验记录(2026-08-04)

## 背景与目标
- 生产基线:e1003(yolov8s × NEU 1800 张)mAP50=0.772(val)/ 0.784(test)
- 目标:突破 0.90(同域真实数据回采是远期主路径,本实验探索数据侧其他可能)

## 一、已证伪路径(本次确认)
| 路径 | 实验 | mAP50 | 结论 |
|---|---|---|---|
| 数据增强四件套 | neu_enhanced_v11s3(97ep) | 0.771 | ≈ 基线,无提升 |
| 数据增强四件套 | neu_enhanced_v8s(78ep) | 0.760 | 低于基线 |
| 跨域补数据 | GC10 合并(历史) | 0.727 | 域偏移,掉点 |
| 强增强 | aug5(历史) | 0.730 | 小数据集扛不住 |

**结论:NEU 增强路径(过采样/copy-paste/伪标签/跨域)全部证伪,瓶颈不在数据量/增强。**

## 二、类别级瓶颈诊断(e1003 test 集)
| 类别 | mAP50 | Precision | Recall | 诊断 |
|---|---|---|---|---|
| crazing | **0.155** | 0.63 | **0.37** | 严重漏检,标注疑似漏标+过度标注 |
| rolled-in_scale | **0.293** | 0.73 | **0.49** | 第二短板 |
| inclusion | 0.468 | 0.74 | 0.77 | 中 |
| pitted_surface | 0.596 | 0.81 | 0.81 | 良 |
| patches | 0.622 | 0.89 | 0.92 | 良 |
| scratches | 0.660 | 0.86 | 0.91 | 良 |
| **整体** | **0.784** | 0.78 | 0.71 | |

**关键发现:两大短板 = crazing + rolled-in_scale,恰好是标注问题最集中的两类。**

## 三、标注问题量化(e1003 推理 val 集 vs GT)
- 共 326 条可疑(195 张图):
  - **误检(无GT匹配)= 218 条** → 模型检出但 GT 没有 = **疑似漏标**
    - 置信度:≥0.7 有 20 条,≥0.5 有 92 条,中位 0.446
    - 按类:inclusion 59 / crazing 55 / rolled-in_scale 48 / scratches 24 / patches 23 / pitted 9
  - **漏检(GT未匹配)= 104 条** → GT 有但模型没检出 = **疑似坏标注或模型弱**
    - 按类:crazing 36 / rolled-in_scale 32 / inclusion 21
  - **大面积方形框(过度标注)= 46 条**(crazing 为主,面积占比最高 92%)
- 训练集 GT:2954 框/1260 图(平均 2.34 框/图)

## 四、修复实验设计:NEU_FIXED(标注补标,只增不改)
- 思路:模型 conf≥阈值且与 GT 最大 IOU<0.45 的框 → 补进 GT(解决漏标)
- 阈值敏感性:
  - conf≥0.9 → 213 框(保守,只补极自信框)
  - conf≥0.85 → 799 框
  - conf≥0.8 → 1335 框
  - conf≥0.55 → 1763 框
- **选定 conf≥0.9(213 框)**:训练集自举有误差放大风险,保守优先
  - 各类:pitted_surface +86 / patches +76 / scratches +15 / crazing +14 / rolled-in_scale +12 / inclusion +10
- 训练脚本:`scripts/train_neu_fixed.py`(与 e1003 同配置,仅换数据集,保证可比)
- 产物:`runs/neu_fixed_v8s/weights/best.pt`

## 五、快速验证(20 epochs 短跑)
- 对照:e1003 基线 epoch20 mAP50 = 0.594
- NEU_FIXED epoch20:**0.723(+22% 显著有效)**
- 完整 100 epochs 训练完成 → 见五bis 最终结论

## 五bis、NEU_FIXED 完整训练最终结论(100 epochs,test 集)
| 指标 | e1003 基线 | NEU_FIXED | 变化 |
|---|---|---|---|
| 整体 mAP50 | 0.784 | 0.765 | -0.02 ⚠️ |
| Precision | 0.778 | 0.674 | -0.10 🔴 |
| **Recall** | 0.714 | **0.771** | **+0.06 🟢** |
| crazing Recall | 0.37 | **0.45** | +0.08 |
| rolled-in_scale Recall | 0.49 | **0.66** | +0.17 |

**结论:补标修复了漏检(两大短板 Recall 大幅提升),但自举引入噪声(Precision -0.10),mAP50 略降。**
- 生产视角:Recall 比 mAP50 更重要(漏检=不良品流出),NEU_FIXED 有实用价值,但暂不替换生产基线
- **下一步:人工标注审查(唯一能同时修 Precision+Recall 的路径)**

## 六、人工标注审查交付物(2026-08-04 16:00)
- `runs/label_review/fix_list_train.csv/txt` — **train 集**修正清单(补标 213 / 忽略 3345 / 核查 141)
- `runs/label_review/report_train.html` — 312 张图可视化(绿=补标,黄=核查)
- 核查项 141 条按类:crazing 66(47%)/ pitted_surface 27 / rolled-in_scale 22 / inclusion 18 → 与类别诊断完全吻合
- **人工确认流程**:看图 → 每个框决定 补/删/缩/留 → 反馈 → 生成 NEU_FIXED2 重训

## 七、用户新增训练路线评估(2026-08-04 15:40)
| 路线 | 脚本 | 可行性 | 结论 |
|---|---|---|---|
| GC10 预训练→微调 | train_gc10_pretrain + train_neu_finetune | 🟡 可行有风险 | 数据就绪;历史 GC10 合并掉点,迁移收益存疑;已 resume 后台跑 |
| Severstal 预训练→微调 | train_severstal_pretrain + convert_severstal | 🔴 不可行 | 数据未下载(需 Kaggle);4 类与 NEU 6 类对不齐;转换脚本未验证 |

## 七bis、迁移学习 + WBF 集成实验最终结论(2026-08-05 上午)
**两条迁移路线均跑完并完成 WBF 集成验证:**

| 模型 | val mAP50 | test mAP50(ultralytics) | test mAP50(统一实现) |
|---|---|---|---|
| e1003 基线(生产) | 0.772 | 0.784 | 0.689 |
| GC10→NEU 微调 | 0.724 | 0.730 | 0.648 |
| Severstal→NEU 微调 | 0.742 | 0.746 | 0.645 |
| **WBF 集成(A+B)** | — | — | **0.673** |

- **WBF 有效但不够**:类别级几乎全面优于单模型(crazing 0.166→0.193 提升最大),整体比最优单模型 +0.025
- **但集成后仍低于基线**(0.673 < 0.689,同一实现)→ 两个较弱模型的互补无法超越更强的单模型
- **结论:迁移学习整条路线证伪**。原因:GC10/Severstal 源域与 NEU 域差异太大,预训练损失了 ImageNet 通用特征,迁移反而掉点
- 脚本:`scripts/wbf_ensemble.py`(WBF 融合 + 统一评估实现)

**模型侧提升路径全部证伪,维持 e1003 为生产模型。唯一可靠突破 = 产线相机到货后回采同域真实数据重训。**

## 八、后续方向(按优先级)
1. **人工标注审查**(fix_list_train + report_train):326 条逐一确认,"漏标→补 / 坏标→删缩" — 最高质量路径
2. **GC10 预训练→微调**:后台验证中(迁移路线对照)
3. **crazing 专项**:核查项 66 条占 47%,修复收益最大
4. **同域真实数据回采**:相机到货后每类≥500张(远期主路径)

---

## 九、广东铝型材同域数据集实验(2026-08-06)

> 阶段七结论"突破需同域真实数据"。下载天池广东铝型材数据集(3826张/3.4GB/2560×1920/无bbox弱标注) → 两条实验全部完成,验证了"无标注→可部署检测"管线。

### 9.1 数据集整理
- 位置:`<LOCAL_ROOT>/ORIN NANO/dataset/`
- 文件名编码坑:zipfile cp437 误解码 UTF-8 → `encode('cp437').decode('utf-8')` 修复
- 去重+归类:2386 → 2213 张有效(train 1994 / val 219,28 类);别名归一(拖烂→拖伤、喷涂碰伤→喷涂划伤)
- 4 个稀有类(油印/驳口/喷涂划伤/返底)仅 1 张图,只能留在 train,val 不评估

### 9.2 实验1: 分类 baseline(YOLOv8s-cls) ✅
- 配置:`train_guangdong_baseline.py`,yolov8s-cls.pt,imgsz=640,batch=16,epochs=80(20 早停),cos_lr,label_smoothing=0.1
- 结果(独立 predict,⚠️ 训练循环 val 显示 top1=5% 不可信):
  - **last.pt top1 = 92.69% (203/219)** ← 最佳
  - best.pt top1 = 87.21% (191/219)
- 强类:正常 96.9%/漏底 100%/桔皮 100%/起坑 100%/凸粉 100%/不导电 100%/脏点 84.6%
- 弱项:擦花 35.7%/碰凹 40%(视觉混淆);小样本类(<3张)0% 属统计噪声
- 产物:`runs/cls/guangdong_baseline/weights/{last,best}.pt`

### 9.3 实验2: GradCAM 弱监督检测(YOLOv8n) ✅
- 思路:分类模型特征(GradCAM 目标层 model[8] 512ch)→ 热力图 → 阈值 0.35+连通域 → 伪 bbox → 训 YOLO 检测
- 工具:`gen_cam_bbox.py`(--sample/--random/--full 三模式);伪框数据集 `dataset/guangdong_detect/`(3411 框,正常类空 label)
- 配置:`train_guangdong_cam_v1.py`,yolov8n.pt,imgsz=640(1024 太慢 8min/epoch 弃用),batch=16,epochs=60(53 中断,best 已存)
- 结果(伪框自洽分数):**mAP50=0.3505 / P=0.690 / R=0.285**,推理 2.6ms@640
  - 强类:擦花 0.995/喷涂划伤 0.995/桔皮 0.913/不导电 0.576
  - 小样本类 AP≈0(数据不足)
- 产物:`runs/detect/guangdong_cam_v1/weights/best.pt`(3M 参数,6.2MB)

### 9.4 训练环境踩坑总结(重要,可复用)
| # | 问题 | 解法 |
|---|---|---|
| 1 | GitHub 权重下载被墙超时 | `curl -x http://<proxy-host>:<proxy-port>` 代理手动下载 |
| 2 | Windows multiprocessing spawn 报错 | 脚本必须 `if __name__=='__main__': freeze_support()` |
| 3 | WorkBuddy safe-delete 钩子拦截 ultralytics 删 cache/rmtree | **`python -S` 启动** + 手动 `PYTHONPATH=...site-packages`;删目录用 PowerShell Remove-Item |
| 4 | 训练循环 val 指标误导(top1=5% vs 实际 92.69%) | **必须独立 `model.predict()` 验证** |
| 5 | CUDA OOM(batch=32/wk=8) | batch=16 + workers=2~4 |
| 6 | imgsz=1024 训练极慢(8min/epoch) | 伪框场景 imgsz=640 足够 |

### 9.5 结论
- 无标注→可部署检测模型管线完整跑通(分类 92.69% → GradCAM 伪框 → 检测 0.35)
- 铝型材同域检测雏形已产出,相机到货(9月)后用真实标注数据微调即可大幅提升
- 报告:`baseline_report_20260806.md` / `detect_report_20260806.md`

